h1 Listing users

table
  thead
    tr
      th Serial number
      th Email
      th Active
      th
      th
      th

  tbody
    - @users.each do |user|
      tr
        td = user.serial_number
        td = user.email
        td = user.active
        td = link_to 'Show', user
        td = link_to 'Edit', edit_user_path(user)
        td = link_to 'Destroy', user, data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New User', new_user_path
